[project]
name = "veloraai"
version = "1.0.0"
description = "VeloraAi backend API"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = ["alembic/versions", ".venv", "web"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle
    "F",    # pyflakes
    "I",    # isort
    "B",    # bugbear
    "UP",   # pyupgrade
    "S",    # bandit security
    "C4",   # comprehensions
    "SIM",  # simplify
    "RUF",
]
ignore = [
    "E501",   # line length is handled by the formatter
    "B008",   # FastAPI Depends() in defaults is the documented pattern
    "SIM105",
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S", "B011"]
"scripts/**" = ["S603", "S607"]
"app/api/v1/system.py" = ["S110"]

[tool.ruff.lint.isort]
known-first-party = ["app", "scripts", "tests"]

[tool.mypy]
python_version = "3.11"
warn_unused_configs = true
ignore_missing_imports = true
disallow_untyped_defs = false
check_untyped_defs = true
warn_redundant_casts = true
warn_unused_ignores = false
exclude = "(alembic/|tests/|web/)"

# Every model is now SQLAlchemy 2.0 `Mapped[...]`, so the whole model layer is
# checked. Services, CRUD, and API modules are added as they are annotated;
# see docs/adr/0002-typing.md for the ratchet.
files = [
    "app/core",
    "app/models",
    "app/schemas",
    "app/crud",
    "app/services/auth_tokens.py",
    "app/services/credential_service.py",
    "app/services/data_export.py",
    "app/services/notification_service.py",
    "app/services/subscription_lifecycle.py",
    "app/services/quota_service.py",
    "app/services/billing_service.py",
    "app/services/audit_service.py",
    "app/tools/credentials.py",
    "app/tools/errors.py",
    "app/tools/base.py",
    "app/tools/policy.py",
    "app/tools/registry.py",
    "scripts",
]

[tool.pytest.ini_options]
asyncio_default_fixture_loop_scope = "function"
markers = [
    "midtrans_e2e: real Midtrans Sandbox tests; skipped unless explicitly enabled",
]
filterwarnings = [
    "ignore::DeprecationWarning:starlette.*",
]

[tool.coverage.run]
source = ["app"]
omit = ["app/static/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]
